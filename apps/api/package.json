{
  "name": "@nisum/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "esbuild src/server.ts --platform=node --bundle --format=esm --packages=external --outfile=dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@nisum/shared-types": "1.0.0",
    "cors": "^2.8.5",
    "express": "^5.0.1"
  }
}
